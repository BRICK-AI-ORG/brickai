############################################################
# 🧱 BRICKAI ENVIRONMENT CONFIGURATION
# Local development (.env.local) and integration testing (.env.test.local)
#
# ⚠️ IMPORTANT:
#  - Never commit these files to Git (they’re ignored by default)
#  - Keep secrets private — do NOT expose secret keys in frontend code
############################################################

############################################################
# 🔗 SUPABASE CONFIGURATION
############################################################

# Supabase project URL (auto-generated when you create your project)
SUPABASE_URL=""
# Exposed to the browser for client-side Supabase usage
NEXT_PUBLIC_SUPABASE_URL=""

# Public (anon) key — safe for client-side use (e.g., in Next.js frontend)
SUPABASE_ANON_KEY=""
# Exposed to the browser for client-side Supabase usage
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Service role key — server-only secret for backend / Edge Functions
SUPABASE_SERVICE_ROLE_KEY=""

# Optional: organization slug (unused, for potential future multi-org setup)
SUPABASE_ORGANIZATION_SLUG="" #Currently not used anywhere

############################################################
# 💳 STRIPE CONFIGURATION
############################################################

# Stripe publishable key — only used in frontend components (e.g., Stripe.js)
# Safe for public exposure, but usually unnecessary unless you use Stripe Elements.
STRIPE_PUBLISHABLE_KEY="" #Currently not used anywhere

# Stripe secret key — required for backend actions (creating sessions, handling webhooks)
STRIPE_SECRET_KEY=""

# Webhook endpoint URL (used for testing or when manually verifying webhook calls)
# Not required if you use `stripe listen` locally.
STRIPE_WEBHOOK_URL="" #Currently not used anywhere 

# Webhook signing secret — verifies that incoming webhooks are truly from Stripe
STRIPE_WEBHOOK_SECRET=""

# If you use a single default plan during signup (optional)
# You can still support plan upgrades/downgrades later through the Stripe Billing Portal.
# STRIPE_PRICE_ID="price_..."

###########################################################
# 🪙 STRIPE PRICE IDS — ACTIVE PRODUCTS ONLY
# Each tier (Starter, Growth, Pro) supports both monthly and annual plans.
############################################################

# Starter Plan
STARTER_MONTHLY_PRICE_ID=   # £32/month
STARTER_ANNUAL_PRICE_ID=     # £299/year

# Growth Plan
GROWTH_MONTHLY_PRICE_ID=     # £89/month
GROWTH_ANNUAL_PRICE_ID=       # £799/year

# Pro Plan
PRO_MONTHLY_PRICE_ID=         # £149/month
PRO_ANNUAL_PRICE_ID=           # £1,399/year

############################################################
# 🧠 OPENAI CONFIGURATION (AI Features in BrickAI)
############################################################

# Secret key for AI task generation or GPT-based features.
# Required by Edge Function: `create-task-with-ai`
OPENAI_API_KEY=""

############################################################
# 🌍 APP + CORS CONFIGURATION
############################################################

# Base app URL — used for Stripe success/cancel URLs and CORS validation
# For production, set this to your deployed frontend URL
APP_URL=""

# Comma-separated list of origins allowed to call your Supabase Edge Functions
# Always include localhost for local dev.
ALLOWED_ORIGINS=""

############################################################
# 🧩 SUPABASE EDGE FUNCTION NOTES
############################################################
# - Supabase automatically injects the following when deployed:
#     SUPABASE_URL
#     SUPABASE_ANON_KEY
#     SUPABASE_SERVICE_ROLE_KEY
#     SUPABASE_DB_URL
#
# - For local Edge Function testing, replicate these manually here.
# - When deploying via Supabase CLI, set secrets using:
#     supabase secrets set KEY=value
